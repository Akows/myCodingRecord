계단식 스타일 시트.

HTML은 태그가 태그를 포함하는 계단식 구조를 지니고 있다.
CSS는 여기에 스타일 요소를 더하여 하나의 웹 페이지를 완성하는 역할을 한다.

HTML + CSS로 만들어진 웹 페이지는 웹 브라우저에게 전달되어 코드가 해석된 다음, 화면에 출력되게 한다.
CSS의 특성상, 독립적으로 존재할 수는 있으되 HTML과 결합되지 않으면 아무런 역할을 하지 못한다.
